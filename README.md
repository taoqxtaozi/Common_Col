# common_col---extract common columns of several alignments based on coordinates
python >= 3.6
Need to install biopython, interval3, collections and multiprocessing
```
pip install biopython interval3 collections collections
```

How to use:
